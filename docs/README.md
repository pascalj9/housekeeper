# `docs/` — deep-dive design notes

This folder holds longer-form documents that don't belong inline in
[`../design.md`](../design.md). The design doc links to them where relevant.

Currently empty — first entries will arrive with later phases (e.g.
`perception.md`, `agent-tools.md`).
