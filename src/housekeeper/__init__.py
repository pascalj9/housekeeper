"""Housekeeper — local-first AI agent for a single IP camera.

This package is intentionally small at this stage: just enough to expose a CLI
and a version string so the rest of Phase 0 can hang off it.
"""

from __future__ import annotations

__all__ = ["__version__"]

__version__ = "0.0.1"
